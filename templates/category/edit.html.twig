{% extends 'base.html.twig' %}

{% block title %}Modifier Catégorie | {{ category.name }}{% endblock %}

{% block body %}
  <div class="container mtCategory w-75">
    <h1 class="h3 font-weight-normal text-center mb-3">Modifier Catégorie</h1>
    {{ include('category/_form.html.twig', {'button_label': ''}) }}
    <div class="text-center">
      <a class="btn btn-md btn-secondary" href="{{ path('category_index') }}"><i class="bi bi-arrow-return-left"></i></a>
      {{ include('category/_delete_form.html.twig') }}
    </div>
  </div>
{% endblock %}
